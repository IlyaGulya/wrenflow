name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 45
    permissions:
      contents: write
    env:
      HAS_SIGNING_CERT: ${{ secrets.DEVELOPER_ID_CERTIFICATE_BASE64 != '' }}
      HAS_NOTARIZE: ${{ secrets.APPLE_ID != '' }}
      IS_RELEASE: ${{ github.event_name == 'release' }}
      IS_BETA: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
      IS_PR: ${{ github.event_name == 'pull_request' }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Skip if release-please commit
        if: env.IS_BETA == 'true'
        id: skip
        run: |
          MSG=$(git log -1 --pretty=%s)
          if echo "$MSG" | grep -qE '^chore\(main\): release'; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Select Xcode 16
        if: steps.skip.outputs.skip != 'true'
        run: sudo xcode-select -s /Applications/Xcode_26.3.app/Contents/Developer

      - name: Install Rust toolchain
        if: steps.skip.outputs.skip != 'true'
        uses: dtolnay/rust-toolchain@7b1c307e0dcbda6122208f10795a713336a9b35a # stable
        with:
          toolchain: stable

      - name: Cache Rust
        if: steps.skip.outputs.skip != 'true'
        uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
        with:
          workspaces: core -> target
          cache-workspace-crates: "true"
          prefix-key: "v1-rust"

      - name: Install build tools
        if: steps.skip.outputs.skip != 'true'
        run: brew install just create-dmg fileicon

      - name: Compute version info
        if: steps.skip.outputs.skip != 'true'
        id: version
        run: |
          BUILD_NUMBER=$(git rev-list --count HEAD)
          echo "build_number=$BUILD_NUMBER" >> "$GITHUB_OUTPUT"

          if [ "${{ env.IS_RELEASE }}" = "true" ]; then
            TAG="${{ github.event.release.tag_name }}"
          else
            # Beta: bump minor from last release tag, append -beta.N
            # Find latest stable tag (no pre-release suffix)
            LATEST_TAG=$(git tag -l 'v*' --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -1)
            LATEST_TAG="${LATEST_TAG:-v0.0.0}"
            COMMITS_SINCE=$(git rev-list "${LATEST_TAG}..HEAD" --count)
            # Parse major.minor from tag, bump minor
            VERSION="${LATEST_TAG#v}"
            MAJOR=$(echo "$VERSION" | cut -d. -f1)
            MINOR=$(echo "$VERSION" | cut -d. -f2)
            NEXT_MINOR=$((MINOR + 1))
            TAG="v${MAJOR}.${NEXT_MINOR}.0-beta.${COMMITS_SINCE}"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Inject build number into Info.plist
        if: steps.skip.outputs.skip != 'true' && env.IS_PR != 'true'
        run: |
          plutil -replace CFBundleVersion -string "${{ steps.version.outputs.build_number }}" Info.plist

      - name: Import signing certificate
        if: steps.skip.outputs.skip != 'true' && env.HAS_SIGNING_CERT == 'true' && env.IS_PR != 'true'
        env:
          CERTIFICATE_BASE64: ${{ secrets.DEVELOPER_ID_CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.DEVELOPER_ID_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        run: |
          echo "$CERTIFICATE_BASE64" | base64 --decode > certificate.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import certificate.p12 -k build.keychain \
            -P "$CERTIFICATE_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" build.keychain >/dev/null 2>&1
          security list-keychains -d user -s build.keychain login.keychain
          rm certificate.p12
          IDENTITY=$(security find-identity -v -p codesigning build.keychain | grep "Developer ID Application" | head -1 | sed 's/.*"\(.*\)".*/\1/')
          echo "WRENFLOW_CODESIGN_IDENTITY=$IDENTITY" >> "$GITHUB_ENV"

      # PR: debug build only (ad-hoc signing)
      - name: Build (PR check)
        if: steps.skip.outputs.skip != 'true' && env.IS_PR == 'true'
        run: just build
        env:
          WRENFLOW_CODESIGN_IDENTITY: "-"

      # Push to main / release: full release build + DMG
      - name: Build release DMG
        if: steps.skip.outputs.skip != 'true' && env.IS_PR != 'true'
        run: |
          rm -rf .build
          just dmg

      - name: Notarize DMG
        if: steps.skip.outputs.skip != 'true' && env.IS_RELEASE == 'true' && env.HAS_NOTARIZE == 'true'
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        run: |
          xcrun notarytool submit build/Wrenflow.dmg \
            --apple-id "$APPLE_ID" \
            --team-id "$APPLE_TEAM_ID" \
            --password "$APPLE_APP_PASSWORD" \
            --wait --timeout 600
          xcrun stapler staple build/Wrenflow.dmg

      - name: Verify codesign
        if: steps.skip.outputs.skip != 'true' && env.IS_PR != 'true'
        run: codesign --verify --deep --strict "build/Wrenflow.app" || true

      - name: Clean up keychain
        if: always()
        run: security delete-keychain build.keychain 2>/dev/null || true

      # Release: upload DMG to existing GitHub Release
      - name: Upload DMG to release
        if: env.IS_RELEASE == 'true'
        run: mv build/Wrenflow.dmg Wrenflow.dmg
      - uses: softprops/action-gh-release@c95fe1489396fe8a9eb87c0abf8aa5b2ef267fda # v2.2.1
        if: env.IS_RELEASE == 'true'
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: Wrenflow.dmg

      # Beta: create pre-release with DMG
      - name: Create beta pre-release
        if: steps.skip.outputs.skip != 'true' && env.IS_BETA == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          mv build/Wrenflow.dmg Wrenflow.dmg
          gh release create "$TAG" Wrenflow.dmg \
            --title "Wrenflow $TAG" \
            --prerelease \
            --notes "Beta build from \`main\`. [Latest stable release](https://github.com/${{ github.repository }}/releases/latest)."
